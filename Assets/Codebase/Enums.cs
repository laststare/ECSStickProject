namespace Codebase
{
    public enum LevelFlowState
    {
        None,
        PlayerIdle,
        StickGrowsUp,
        StickFalls,
        PlayerRun,
        CameraRun, 
        GameOver,
        Start,
    }

    public enum MainMenuButton
    {
        StartGame,
        RestartGame,
        BackToStartScreen
    }
}