namespace Codebase
{
    public enum LevelFlowState
    {
        None,
        PlayerIdle,
        StickGrowsUp,
        StickFalls,
        PlayerRun,
        CameraRun, 
        GameOver
    }

    public enum MainMenuButton
    {
        StartGame,
        RestartGame,
        BackToStartScreen
    }
}